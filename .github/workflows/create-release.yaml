name: Create release

on:
  repository_dispatch:
    types: [create-release]

jobs:
  create-release:
    runs-on: ubuntu-latest

    steps:
      - name: Create release
        run: |
          echo "Creating release for version ${{ github.event.client_payload.version }}"