name: Create release

on:
  release:
    types: [created]

jobs:
  create-release:
    runs-on: ubuntu-latest
    
    steps:
      - name: Create release
        run: |
          echo "Creating release for version ${{ github.event.release.tag_name }}"